---
import ActualitesListPage from '../../../components/actualites/ActualitesListPage.astro'
import { getPublishedActualites } from '../../../lib/actualites'

const lang = 'en' as const
const posts = await getPublishedActualites(lang)
---

<ActualitesListPage lang={lang} posts={posts} />
